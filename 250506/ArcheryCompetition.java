public class ArcheryCompetition {
    public int[] solution(int n, int[] info) {
        int[] answer = {};
        int apeach = 0; // 어피치 점수
        int lion = 0;   // 라이언 점수 (나중에 점수 비교 해보려고)
        int arrow = 0;  // 쏜 화살

        for(int i = 0; i < info.length; i++) {  // 어피치가 맞춘 거 보면서..
            if()
        }

        return answer;
    }
}